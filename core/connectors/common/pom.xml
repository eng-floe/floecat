<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>ai.floedb.floecat</groupId>
    <artifactId>floecat</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <relativePath>../../..</relativePath>
  </parent>

  <artifactId>floecat-connectors-common</artifactId>
  <packaging>jar</packaging>
  
  <properties>
    <iceberg.version>1.10.0</iceberg.version>
    <parquet.version>1.16.0</parquet.version>
    <hadoop.version>3.4.2</hadoop.version>
  </properties>
  
  <dependencies>

    <!-- Typesystem dependency (LogicalType, LogicalComparators, ValueEncoders, etc) -->
    <dependency>
      <groupId>ai.floedb.floecat</groupId>
      <artifactId>floecat-types</artifactId>
      <version>${project.version}</version>
    </dependency>

    <!-- Proto (stats, NDV, value encoders, etc) -->
    <dependency>
      <groupId>ai.floedb.floecat</groupId>
      <artifactId>floecat-proto</artifactId>
      <version>${project.version}</version>
    </dependency>
    
    <!-- Connectors SPI -->
    <dependency>
      <groupId>ai.floedb.floecat</groupId>
      <artifactId>floecat-connectors-spi</artifactId>
      <version>${project.version}</version>
    </dependency>
    
    <!-- Heavy libraries connectors-common really uses -->
    <dependency>
      <groupId>org.apache.parquet</groupId>
      <artifactId>parquet-hadoop</artifactId>
      <version>${parquet.version}</version>
    </dependency>

    <dependency>
      <groupId>org.apache.parquet</groupId>
      <artifactId>parquet-column</artifactId>
      <version>${parquet.version}</version>
    </dependency>

    <dependency>
      <groupId>org.apache.datasketches</groupId>
      <artifactId>datasketches-java</artifactId>
      <version>9.0.0</version>
    </dependency>

    <!-- Needed for reading parquet via Iceberg IO wrappers -->
    <dependency>
      <groupId>org.apache.iceberg</groupId>
      <artifactId>iceberg-core</artifactId>
      <version>${iceberg.version}</version>
    </dependency>
    
    <!-- Hadoop filesystem + config -->
    <dependency>
      <groupId>org.apache.hadoop</groupId>
      <artifactId>hadoop-common</artifactId>
      <version>${hadoop.version}</version>
      <exclusions>
        <exclusion>
          <groupId>org.apache.httpcomponents</groupId>
          <artifactId>httpclient</artifactId>
        </exclusion>
        <exclusion>
          <groupId>org.slf4j</groupId>
          <artifactId>slf4j-reload4j</artifactId>
        </exclusion>
        <exclusion>
          <groupId>ch.qos.reload4j</groupId>
          <artifactId>reload4j</artifactId>
        </exclusion>
        <exclusion>
          <groupId>commons-logging</groupId>
          <artifactId>commons-logging</artifactId>
        </exclusion>
        <exclusion>
          <groupId>org.slf4j</groupId>
          <artifactId>slf4j-log4j12</artifactId>
        </exclusion>
        <exclusion>
          <groupId>log4j</groupId>
          <artifactId>log4j</artifactId>
        </exclusion>
        <exclusion>
          <groupId>javax.xml.stream</groupId>
          <artifactId>stax-api</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
  </dependencies>

</project>