// Copyright 2026 Yellowbrick Data, Inc.
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

syntax = "proto3";

package ai.floedb.floecat.account;

option java_multiple_files = true;
option java_package = "ai.floedb.floecat.account.rpc";
option java_outer_classname = "AccountProto";

import "floecat/common/common.proto";

import "google/protobuf/timestamp.proto";
import "google/protobuf/field_mask.proto";

message Account {
  ai.floedb.floecat.common.ResourceId resource_id = 1;
  string display_name = 2;
  optional string description = 3;
  google.protobuf.Timestamp created_at = 10;
  // Free-form key/value tags used to scope which floecat instance manages the
  // account (e.g. a CI run stamps `ci-run=<run-id>`; see the planner tag filter).
  map<string, string> tags = 99;
}

message AccountSpec {
  optional string display_name = 1;
  optional string description = 2;
  map<string, string> tags = 99;
}

// ====== Requests / Responses ======

message ListAccountsRequest {
  ai.floedb.floecat.common.PageRequest page = 1;
}

message ListAccountsResponse {
  repeated Account accounts = 1;
  ai.floedb.floecat.common.PageResponse page = 2;
}

message CreateAccountRequest {
  AccountSpec spec = 1;
  // Optional resource id for external account synchronization.
  optional ai.floedb.floecat.common.ResourceId account_id = 2;
  ai.floedb.floecat.common.IdempotencyKey idempotency = 98;
}

message CreateAccountResponse {
  Account account = 1;
  ai.floedb.floecat.common.MutationMeta meta = 2;
}

message GetAccountRequest {
  ai.floedb.floecat.common.ResourceId account_id = 1;
}

message GetAccountResponse {
  Account account = 1;
}

message UpdateAccountRequest {
  ai.floedb.floecat.common.ResourceId account_id = 1;
  AccountSpec spec = 2;
  google.protobuf.FieldMask update_mask = 3;
  ai.floedb.floecat.common.Precondition precondition = 98;

}

message UpdateAccountResponse {
  Account account = 1;
  ai.floedb.floecat.common.MutationMeta meta = 2;
}

// Removing an account also removes everything it owns: connectors and their external
// credentials, catalogs, and every namespace, table and view beneath them.
//
// An account id that has no pointer is reported as deleted rather than NOT_FOUND, and the
// precondition is not consulted on that path. Cleanup runs after the pointer is removed and can
// die part-way, so this is also the path a retry of such a delete lands on, and it sweeps again
// there. "Never existed" and "I removed it on an earlier attempt" are indistinguishable once the
// pointer is gone -- nothing tombstones it -- and answering NOT_FOUND is what a teardown scheduler
// reads as "done", which is what would make a partial sweep's orphans permanent. So the answer is
// the one that is safe to be wrong about, and the sweep runs either way. It is idempotent: a
// second pass finds nothing and removes nothing.
message DeleteAccountRequest {
  ai.floedb.floecat.common.ResourceId account_id = 1;
  ai.floedb.floecat.common.Precondition precondition = 98;
}

message DeleteAccountResponse {
  ai.floedb.floecat.common.MutationMeta meta = 1;
}

// ====== Service ======

service AccountService {
  rpc ListAccounts (ListAccountsRequest) returns (ListAccountsResponse);
  rpc GetAccount (GetAccountRequest) returns (GetAccountResponse);
  rpc CreateAccount (CreateAccountRequest) returns (CreateAccountResponse);
  rpc UpdateAccount (UpdateAccountRequest) returns (UpdateAccountResponse);
  rpc DeleteAccount (DeleteAccountRequest) returns (DeleteAccountResponse);
}
